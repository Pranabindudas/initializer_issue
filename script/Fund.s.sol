pragma solidity ^0.8.0;
import "forge-std/Script.sol";
contract Fund is Script {
    function run() external payable {
        // Nothing – just receive ETH when running with --broadcast and --private-key
    }
}